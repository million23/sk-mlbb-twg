# Legacy libs

Domain helpers for the abandoned `/legacy` UI and related components.

Shared infrastructure stays at `@/lib` (`utils`, `public-site`, `pocketbase`, `query-client`, `rate-limited-api`).
